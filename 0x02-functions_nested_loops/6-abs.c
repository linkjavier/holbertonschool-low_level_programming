#include "holberton.h"

/**
 * _abs - Function that computes the absolute value of an integer.
 *
 * @int: an int
 *
 *
 * Return: @x
**/


int _abs(int x)
{

	if (x < 0)
	{
		x = -x;
		return (x);
	}
	else
	{
		return (x);
	}
}
