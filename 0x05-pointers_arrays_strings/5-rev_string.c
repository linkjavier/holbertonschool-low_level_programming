#include "holberton.h"

/**
 * rev_string - Reverses a string.
 *
 * @s: Address of a string.
 */

void rev_string(char *s)
{
	int i;
	char aux[];
	
	while (*(s + i) != 0)
	{
		i++;
	}
	while (i >= 0);

