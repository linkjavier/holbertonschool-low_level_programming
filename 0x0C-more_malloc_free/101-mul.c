#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

void *_mul_dynamic(*num1, *num2)
{
	int prod, tmp = 0;

	for(i = 0, i < mul2(num1)   ,i++)
	prod = ((num1 % 10) + tmp) * (num2 % 10);
	num1 = num1 / 10;
	num2 = num2 / 10;
	if (prod % 10)
	ptr[i] = prod % 10;
	tmp = prod / 10;
}
